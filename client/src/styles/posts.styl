.ele {

  .delete {color: white; background: #d9534f; height: 25px; width: 25px;float: right;
  line-height:25px;text-align:center;border-radius: 5px}

  border: none

  b {color: #ec407a; font-weight: 600}

  .content-ele {
    padding-left: 60px;
    border-left: 8px solid #eee;
    margin: 1.0em 0 1.25em;
    font-style: italic;

    &::before{content: '“'; color: #aaa; letter-spacing: 2px; padding-right:1px}
    &::after{content: "”"; color: #aaa; letter-spacing: 2px; padding-left:1px}
    }
  }

  div.post-ele {
    margin-bottom:75px;
    padding: 0;
  }
